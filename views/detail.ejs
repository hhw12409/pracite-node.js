<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/css/styles.css">
  <title>node.js practice</title>
</head>
<body>
  <%- include('nav.html')%>
  <div class="container">
    <h1><%=data.title%></h1>
    <p><%=data.content%></p>
    <span>Write by <%=data.name%></span></br>
    <p><%=data.date%></p>
  </div>
  <a href="/edit/<%=data._id%>">
    <button class="edit">수정하기</button>
  </a>
  <button class="delete" data-id="<%=data._id%>">삭제하기</button>


  <script src="https://code.jquery.com/jquery-3.4.1.min.js"></script>

    <script>

      const btn = document.querySelector('.delete')
      btn.addEventListener('click', () => {
        const postNumber = event.target.dataset.id
        $.ajax({
          method : 'DELETE',
          url : '/delete',
          data : { _id : postNumber }
        }).done(function(result) {
          window.location = '/list';
        }).fail(function() {
          console.log('fail')
        })
      })

    </script>
</body>
</html>